1. Write a docker-compose file, defining a postgres server
2. Write a simple python or go program to connect to a database
3. Setup github repository
4. Write a test to execute a select against the given database
5. Setup a github YAML file, to build the code, start the database server, run the tests
